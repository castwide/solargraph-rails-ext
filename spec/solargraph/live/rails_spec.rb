require "spec_helper"

RSpec.describe Solargraph::Live::Rails do
  it "has a version number" do
    expect(Solargraph::Live::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
